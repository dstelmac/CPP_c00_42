#include "phoneBook.hpp"

PhoneBook::PhoneBook() {
	set_index(0);
};

PhoneBook :: ~PhoneBook() {};

int PhoneBook::addContact() {

	if (_index < 8) {

		std::string FirstName, LastName, Nickname, PhoneNumber,DarkSecret;
		std::cout<<"Enter the contact details:"<<srd::endl;
		std::cout<<"First Name: ";
		std::cin>>FirstName;
		std::cout<<

	}


}